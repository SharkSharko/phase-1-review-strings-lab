// Define the current user
const currentUser = 'Emmanuel Melilei';

// Create a welcome message using string interpolation
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

// Convert the welcome message to uppercase for excitement
const excitedWelcomeMessage = welcomeMessage.toUpperCase();

// Create a short greeting using only the first initial of the current user
const shortGreeting = `Welcome, ${currentUser.slice(0, 1)}!`;


